export class Ingredient {
  // This is a shortcut for defining properties set with constructor
  // TODO - consider adding a unit of measure
  constructor(public name: string, public amount: number ){}
}